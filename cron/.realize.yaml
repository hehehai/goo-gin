settings:
  legacy:
    force: false
    interval: 0s
schema:
- name: cron
  path: .
  commands: {}
  watcher:
    extensions:
    - go
    paths:
    - /
    ignore:
      paths:
      - .git
      - .realize
      - vendor
